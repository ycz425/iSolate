"use client"

import { useState } from "react"

export default function TaskManager() {
    const [tabs, setTabs] = useState([])   // POPULATE DATABASE WITH SAMPLE DATA
                                           // (but first figure out how to use Auth0 with database and how to make calls to database from next js)
                                           // decided approach: make api endpoints here (for complex queries/operation) and make elementary database queries with adapter
    return (
        <>
    
        </>
    )
}